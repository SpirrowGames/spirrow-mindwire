# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / TypeScript
node_modules/
*.log
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
build/
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code (local-only state)
.claude/settings.local.json

# MindWire runtime data (never commit)
spirrow-mindwire-data/

# Visual Studio (local-only IDE state)
.vs/

# Spec-delivery pin (untracked-by-design — SPEC-2026-08-11-design-spec-delivery
# D-5 / item I-4). The dispatcher writes `.mindwire/pin` into the working tree
# and it must never be committed. Per OBL-SPEC-PIN, ignoring the directory does
# not touch the pin file itself on disk (A-27 of that spec).
.mindwire/
